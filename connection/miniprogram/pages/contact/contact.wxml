<!--pages/contact/contact.wxml-->
<view class="box">
  <!-- <view class="header">
    <text class="title">联系我们</text>
  </view> -->
  <!-- 
  <view class="contact-info">
    <text class="info-title">联系方式</text>
    <text class="info-item">电话：{{contact.phone}}</text>
    <text class="info-item">邮箱：{{contact.email}}</text>
    <text class="info-item">地址：{{contact.address}}</text>
  </view> -->
  <view class="box-head">
    <text class="label">请输入您的留言</text>
    <textarea maxlength='-1' class="input_box" type="text" confirm-type="done" value="{{messageContent}}" bindinput="onMessageInput"></textarea>
    <view style="height: 40rpx;"></view>
    <button class="confirm_button" type="primary" bind:tap="submitMessage">提交留言</button>
    <view style="margin: 40rpx auto;width: 90%;color:  #8e959b;  ">-- 历史留言 --</view>
  </view>

  <!-- <view class="messages-section">
    <text class="messages-title">历史留言</text>
    <view class="message-item" wx:for="{{messages}}" wx:key="_id">
      <text class="message-content">{{item.content}}</text>
      <text class="message-time">{{item.createTime}}</text>
    </view>
  </view> -->

  <block wx:if="{{messages.length>0}}">
    <scroll-view class="scroll_class box-scroll" scroll-y="true" wx:key="_id">
      <block wx:for="{{messages}}" >
        <text class="message-content">{{item.content}}</text>
        <text class="message-time">{{item.createTime}}</text>
      </block>
    </scroll-view>
  </block>
  <view class="scroll_class" wx:else>暂无数据</view>
</view>